🐙 Criar no GitHub

Na prática, o fluxo mais comum é:

Criar localmente (como acima).

Inicializar um repositório git:

cd stone-desafio
git init


Criar o repositório no GitHub (pelo site).

Conectar o repositório remoto:

git remote add origin git@github.com:seu-usuario/stone-desafio.git


Subir tudo:

git add .
git commit -m "estrutura inicial do desafio"
git branch -M main
git push -u origin main
